# Build output.
build
dist

# Contract fixtures are newline-delimited JSON, not a single JSON document.
../../contracts/bridge/*.ndjson
../../contracts/bridge/bridge-v1.schema.json

# TypeScript emitted byte-for-byte by the Rust contract generator
# (crates/devhub-app-core/src/bin/generate_bridge_contract.rs).
src/generated/bridge/index.ts
