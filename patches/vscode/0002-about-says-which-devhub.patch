diff --git a/src/vs/base/common/product.ts b/src/vs/base/common/product.ts
index 46881e33054..d40105d9370 100644
--- a/src/vs/base/common/product.ts
+++ b/src/vs/base/common/product.ts
@@ -98,6 +98,10 @@ export interface IDictationRuntimeProductConfig {
 
 export interface IProductConfiguration {
 	readonly version: string;
+	// DevHub: the version of the product the user installed, where `version`
+	// above is the version of the VS Code inside it. Set by DevHub's
+	// product.json; absent in stock builds, where the two are the same thing.
+	readonly hostVersion?: string;
 	readonly date?: string;
 	readonly quality?: string;
 	readonly commit?: string;
diff --git a/src/vs/platform/dialogs/electron-browser/dialog.ts b/src/vs/platform/dialogs/electron-browser/dialog.ts
index ec26804c300..f3c9b81c4d2 100644
--- a/src/vs/platform/dialogs/electron-browser/dialog.ts
+++ b/src/vs/platform/dialogs/electron-browser/dialog.ts
@@ -11,7 +11,13 @@ import { IProductService } from '../../product/common/productService.js';
 import { process } from '../../../base/parts/sandbox/electron-browser/globals.js';
 
 export function createNativeAboutDialogDetails(productService: IProductService, osProps: IOSProperties): { title: string; details: string; detailsToCopy: string } {
-	let version = productService.version;
+	// DevHub: `version` is the version of the VS Code inside the product, and on
+	// its own it makes this dialog disagree with the one the OS shows for the
+	// application. When the product states a version of its own, that one leads
+	// and VS Code's is named as what it is.
+	let version = productService.hostVersion
+		? `${productService.hostVersion} (VS Code ${productService.version})`
+		: productService.version;
 	if (productService.target) {
 		version = `${version} (${productService.target} setup)`;
 	} else if (productService.darwinUniversalAssetId) {
