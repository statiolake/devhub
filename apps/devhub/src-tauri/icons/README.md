# Temporary foundation icon

This placeholder exists only so Tauri's development context can be compiled
while the final DevHub vector master and generated icon set remain owned by the
Q5.3 brand task. Replace `icon.png` there; do not use this placeholder as the
release identity.
